TigerVNC Xvnc for the private browser display

Official upstream release:
https://github.com/TigerVNC/tigervnc/releases/tag/v1.16.2
Official generic Linux archive linked by that release:
https://downloads.sourceforge.net/project/tigervnc/stable/1.16.2/tigervnc-1.16.2.x86_64.tar.gz
SHA-256: 5b70c84baefc09a030cfc78315c34ccb55b2a0dde4092b7da67a1962c5f0dea6
Archive bytes: 15042988
Xvnc member: tigervnc-1.16.2.x86_64/usr/bin/Xvnc (13821240 bytes)
Password helper: tigervnc-1.16.2.x86_64/usr/bin/vncpasswd
Install helper as /usr/local/bin/tigervncpasswd to avoid package-name conflicts.

Read-only ELF inspection found maximum required GLIBC symbol version 2.28,
compatible with bookworm's 2.36. The binary contains -displayfd and -rendernode.
Linux ldd and actual startup remain required before deployment.

Suggested Docker installation (keep existing x11vnc and Xvfb):

RUN apt-get update && apt-get install -y --no-install-recommends \
      libgbm1 libxcvt0 libxshmfence1 libgnutls30 libnettle8 libhogweed6 \
      libgmp10 libpam0g libsystemd0 x11-utils mesa-utils \
    && rm -rf /var/lib/apt/lists/* \
    && curl -fL https://downloads.sourceforge.net/project/tigervnc/stable/1.16.2/tigervnc-1.16.2.x86_64.tar.gz -o /tmp/tigervnc.tar.gz \
    && echo '5b70c84baefc09a030cfc78315c34ccb55b2a0dde4092b7da67a1962c5f0dea6  /tmp/tigervnc.tar.gz' | sha256sum -c - \
    && tar -xzf /tmp/tigervnc.tar.gz -C /tmp tigervnc-1.16.2.x86_64/usr/bin/Xvnc tigervnc-1.16.2.x86_64/usr/bin/vncpasswd \
    && install -m 755 /tmp/tigervnc-1.16.2.x86_64/usr/bin/Xvnc /usr/local/bin/Xvnc \
    && install -m 755 /tmp/tigervnc-1.16.2.x86_64/usr/bin/vncpasswd /usr/local/bin/tigervncpasswd \
    && rm /tmp/tigervnc.tar.gz \
    && rm -rf /tmp/tigervnc-1.16.2.x86_64

Also retain upstream copyright/license notices from the release archive when
integrating its binary into the image. Inspect archive paths before selecting
them; do not copy unrelated viewers, server services, or system configuration.

Python integration:
from display import start_display
process, environment, metadata = start_display(engine_config, runtime, engine)

DOUBLETAKE_DISPLAY_BACKEND: auto (default), xvfb, or xvnc.
auto selects Xvnc when its binary and an accessible render node are present,
requires a successful DRI3 query, and stops a failed display before Xvfb fallback.
Forced xvnc on GPU-free CI uses -rendernode '' and must not silently fall back.

The Xvnc display uses -rfbport -1, so it has no TCP RFB listener. Upstream
requires at least one incoming endpoint. Its RFB endpoint is therefore a Unix
socket inside the worker's private 0700 runtime, with socket mode 0600 and
SecurityTypes=VncAuth only. A fresh eight-byte password is passed to the pinned
tigervncpasswd -f helper on stdin; its eight-byte output is stored in an
exclusive-created mode-0600 file. Neither password is logged or placed in
process arguments. These private files disappear with the worker runtime.
X11 TCP is disabled and MIT-MAGIC-COOKIE-1 authentication uses the worker's
private Xauthority. Existing x11vnc preview authentication and its native-control
viewonly guard remain unchanged.

Validation needed:
- ldd /usr/local/bin/Xvnc and tigervncpasswd have no missing dependencies.
- Disposable network-free startup verifies mode-0600 socket and VncAuth-only RFB.
- Xvnc forced in native and diagnostic integration, including input/paste/close.
- CPU-only CI reports backend=xvnc; DRI3=false is expected without render node.
- Live Intel host reports backend=xvnc, DRI3=true, Intel GL renderer.
- Compare YouTube dropped-frame deltas and sender throughput before/after.
- Verify existing HEVC doorbell rendering, login persistence and TV audio.

Bookworm's tigervnc package is 1.12.0 (no new DRI3 support). The current
bookworm-backports amd64 Packages.xz has no tigervnc-standalone-server entry.
Trixie's package is 1.15.0+dfsg-2.1~deb13u1 and requires glibc >=2.38; copying
that Debian binary into bookworm is unsuitable. The official generic archive
avoids changing the runtime distribution or compiling a second X server.
